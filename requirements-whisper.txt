# Install this separately only when you want Whisper AI captions.
# WARNING: pulls ~2.5GB of PyTorch — not suitable for Render free tier.
#
# Usage:
#   pip install -r requirements.txt
#   pip install -r requirements-whisper.txt
#
# On Azure ACI / local with enough disk:
#   pip install -r requirements.txt -r requirements-whisper.txt

openai-whisper
ffmpeg-python
